<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
                    xmlns:converters="clr-namespace:TinyTODO.App.Windows.Converters"
                    >
    <DataTemplate x:Key="ToDoItemDataTemplate">
        <Grid Background="AliceBlue">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="50"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <DockPanel LastChildFill="True" MaxWidth="1500"  Grid.Column="0" >
                <CheckBox IsChecked="{Binding IsCompleted}"/>
            </DockPanel>
            <DockPanel LastChildFill="True" MaxWidth="1500" Background="Beige" Grid.Column="1" >
                <ContentControl Content="{Binding .}" Style="{StaticResource ToDoItemDisplayStyle}" Background="Wheat"/>
            </DockPanel>
            <DockPanel Grid.Column="2" >
                <StackPanel Background="Azure">
                    <TextBlock Text="{Binding ActiveWindowTitle}"></TextBlock>
                    <TextBlock Text="{Binding CreatedDateTime}"></TextBlock>
                </StackPanel>
            </DockPanel>
        </Grid>
    </DataTemplate>
</ResourceDictionary>