<UserControl x:Class="ToDoLite.App.Windows.Views.TagView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:viewmodel="clr-namespace:ToDoLite.App.Windows.ViewModel"
             xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:controls="clr-namespace:ToDoLite.App.Windows.Controls"
             xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
             xmlns:utilities="clr-namespace:ToDoLite.App.Windows.Utilities"
             d:DataContext="{d:DesignInstance Type=viewmodel:TagViewModel}"
             mc:Ignorable="d" Width="Auto" Height="Auto">
        <StackPanel Orientation="Horizontal" ToolTipService.ToolTip="{Binding Description}">
            <Rectangle Width="10" Height="Auto" Fill="{Binding BackColor}" />
            <TextBlock Text="{Binding Name}" Margin="4,0,0,0"/>
        </StackPanel>
</UserControl>
